# Urbem-Downscalling-tool-and-Proxy-Creation
This folder presents the codes necessary to create the proxy layers of each GNFR sector of CAMS emissions (area and points). It also contains the pipeline and User interface codes to downscalle emissions with these proxies with a specific bounding box :)
