# --- Proxy pipeline (proxy/entry.py, sector pipelines, dataset_loaders) ---
numpy>=1.20
PyYAML>=6.0
pyproj>=3.0
xarray>=2022.1
netCDF4>=1.6
rasterio>=1.3
pandas>=1.5
geopandas>=0.12
shapely>=2.0
scipy>=1.7
openpyxl>=3.0
matplotlib>=3.5
Pillow>=9.0
folium>=0.14

# --- OSM preprocess (INPUT/Preprocess/OSM/create_osm_sector_packages.py) ---
# Also needs osmium-tool on PATH for bbox extract on large PBFs (https://osmcode.org/osmium-tool/)
osmium>=3.6
tqdm>=4.60

# --- UrbEm Visualizer (Flask UI + downscaling) ---
flask>=2.0
pywebview>=4.0
